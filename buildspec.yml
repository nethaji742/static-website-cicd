version: 0.2

phases:
  build:
    commands:
      - echo "No build needed for static site"

  post_build:
    commands:
      - echo "Deploying to S3"
      - aws s3 sync . s3://nethajisfdemo --delete --exclude ".git/*" --exclude "buildspec.yml"

artifacts:
  files:
    - '**/*'
