version: 0.2

phases:
  build:
    commands:
      - echo "Build started"
  post_build:
    commands:
      - echo "Deploying to S3"
      - aws s3 sync . s3://nethajisfdemo --delete

artifacts:
  files:
    - '**/*'
